from collections import deque# append, pop, popleft and appendleft 
d=deque()
for _ in range(input()):
    com = raw_input().split()
    if com[0] == 'append':
        d.append(com[1])
    elif com[0] == 'appendleft':
        d.appendleft(com[1])
    elif com[0] == 'pop':
        d.pop()
    elif com[0] == 'popleft':
        d.popleft()
print " ".join(d)       
